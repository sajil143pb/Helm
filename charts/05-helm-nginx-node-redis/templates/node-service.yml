apiVersion: v1
kind: Service
metadata:
  name: node-service
spec:
  selector:
    app: node
  ports:
    - protocol: TCP
      name: metrics
      port: 80
      targetPort: {{ .Values.node.port }}
  type: ClusterIP
